<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to 'App', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav w-100">
      <% if logged_in? %>
        <li class="nav-item dropdown ml-auto">
          <a class="nav-link dropdown-toggle" id="authentication-dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Account
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="authentication-dropdown">
            <%= link_to 'Logout', authentication_path, method: :delete, class: 'dropdown-item', remote: true %>
            <%= link_to 'Update Account', account_path, class: 'dropdown-item' %>
          </div>
        </li>
      <% else %>
        <li class="nav-item ml-auto">
          <%= link_to 'Login', authentication_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
